import * as TYPES from '../constants'

//导出加一的方法e
export function mini_menu() {
    return {
        type: TYPES.MINI_MENU
    }
}
//导出减一的方法
export function max_menu() {
    return {
        type: TYPES.MAX_MENU
    }
}